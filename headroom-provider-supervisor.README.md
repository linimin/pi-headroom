# headroom-provider-supervisor

This project was moved out of `~/.pi/agent/extensions/` into a standalone local project at `~/dev/pi-headroom`.

See [README.md](./README.md) for the current behavior and usage.
